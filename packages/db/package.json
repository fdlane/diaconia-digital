{
  "name": "@diaconia/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "pnpm typecheck",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "node --env-file=../../.env --import tsx src/seed.ts",
    "db:reset": "node --env-file=../../.env --import tsx src/reset.ts",
    "zero:generate": "zero-deploy-permissions --schema-path ./src/zero/schema.ts --output-file ./src/zero/permissions.sql --output-format sql --app-id diaconia_digital"
  },
  "dependencies": {
    "@rocicorp/zero": "^0.23.2025090100",
    "drizzle-orm": "^0.38.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.30.2",
    "drizzle-zero": "^0.1.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
